#version 450

layout(location = 0)
	in vec4 aPosition;
uniform mat4 uModelMatrix, uViewProjMatrix;

out vec2 vTexCoord;

void main()
{
	vec4 tempPos = uViewProjMatrix * uModelMatrix * aPosition;
	gl_Position = tempPos;
	//Below is accurate if reading direct information but FT data
	//DOES NOT go negative unlike the range if I was using my own audio
	//pass.
//	vTexCoord = vec2(sin(aPosition.x), sin(aPosition.y));
	vTexCoord = tempPos.xy * 0.5 + 0.5;
}
